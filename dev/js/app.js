//Refrencing the MDN "createElement() documentation for implemenation"
document.window.onload = addElement;
    function addElement() {
        const newMessage = document.createElement("input");
        const newContent = document.createTextNode("Enter a matrix size: ");
        const newType = document.createElement("Type=")
        const input = document.input.newType("integers");
    }

window.onload = () => {
    window.alert('Enter a matrix size: ' );
    alert('Enter a matrix size: ' );
};
